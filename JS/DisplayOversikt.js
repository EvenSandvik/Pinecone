function overview(){
  let kommuneBefolkning = befolkning.data.elementer;
  let koms = befolkning.getNames();
  koms.sort();
  let alpha = ["A", "B", "D", "E", "F", "G", "H", "I", "J", "K", "L", "M", "N", "O", "P", "R", "S", "T", "U", "V", "Ø", "Å"];

  //Går gjennom alle bokstavane
  for(let boks in alpha){


  //Går gjennom alle kommunenavna
  for(let navn in koms){

  }
}
}
